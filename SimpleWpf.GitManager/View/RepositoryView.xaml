<UserControl x:Class="SimpleWpf.GitManager.View.RepositoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimpleWpf.GitManager.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid ColumnDefinitions="*, Auto" DataContext="{Binding TabDataContext}">
        <GroupBox Grid.Column="1" Header="Git Repository Info" HorizontalAlignment="Stretch" Style="{StaticResource GroupBoxStyle}">
            <Grid ColumnDefinitions="Auto, *" RowDefinitions="Auto, Auto, Auto, Auto, Auto, Auto" HorizontalAlignment="Stretch">

                <TextBlock Text="Name" Style="{StaticResource RepoListBoxItemLabelTextBlock}" />
                <TextBlock Text="Directory" Grid.Row="1" Style="{StaticResource RepoListBoxItemLabelTextBlock}" />
                <TextBlock Text="Last Commit (local)" Grid.Row="2" Style="{StaticResource RepoListBoxItemLabelTextBlock}" />
                <TextBlock Text="Last Commit (remote)" Grid.Row="3" Style="{StaticResource RepoListBoxItemLabelTextBlock}" />
                <TextBlock Text="Last Fetch" Grid.Row="4" Style="{StaticResource RepoListBoxItemLabelTextBlock}" />

                <TextBlock Text="{Binding Name}" Grid.Column="1" Style="{StaticResource RepoListBoxItemTextBlock}" />
                <TextBlock Text="{Binding BaseDirectory}" Grid.Column="1" Grid.Row="1" Style="{StaticResource RepoListBoxItemTextBlock}" />
                <TextBlock Text="{Binding LastCommitLocal}" Grid.Column="1" Grid.Row="2" Style="{StaticResource RepoListBoxItemTextBlock}" />
                <TextBlock Text="{Binding LastCommitRemote}" Grid.Column="1" Grid.Row="3" Style="{StaticResource RepoListBoxItemTextBlock}" />
                <TextBlock Text="{Binding LastFetch, StringFormat='{}{0:yyyy-MM-dd hh:mm:ss tt}'}" Grid.Column="1" Grid.Row="4" Style="{StaticResource RepoListBoxItemTextBlock}" />
                
            </Grid>
        </GroupBox>

        <ListBox ItemsSource="{Binding Log}"
                 ItemContainerStyle="{StaticResource ListBoxItemContainerStyle}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Message}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
    </Grid>
</UserControl>
